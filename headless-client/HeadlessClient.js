import { ApiClient } from "@drupal-api-client/api-client";

export class HeadlessClient extends ApiClient {
  // Set correct default API prefix
  // Create a getView method
  // create a getContentType method
}
